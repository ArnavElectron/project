CREATE DATABASE Electronincs_list_componets;
CREATE TABLE Inventory
    name VARCHAR(25) NOT NULL,
    iteem_type VARCHAR(25) NOT NULL,
    Model_no VARCHAR(10),
    Quantity INT(4),
    datasheet VARCHAR(max),
    max_voltage VARCHAR(4),
INSERT INTO Inventor values(
    
)
